@app.route('/roomDetails/<int:roomId>')
def getRoombyId(roomId):

    cursor = mysql.connection.cursor()  # cursor help flask to interact with Database
    cursor.execute('''SELECT f.flat_id as 'SNO',f.flat_title as 'Title',f.address as 'Address',f.date as 'Date',f.price as 'Price',f.area as 'Area',f.no_room as 'No of Rooms',f.floor as 'Floor',f.parking_car as 'Parking Car',f.parking_bike as 'Parking Bike',f.description as 'Description', u.first_name as 'Full Name', u.phone_number as 'Contact' FROM `flat` as f join `users` as u on f.user_id = u.user_id WHERE f.flat_id=%s;''', (roomId,))
    room = cursor.fetchone()
    cursor.close()

    # cursor provide flask to interact with Database
    cursor = mysql.connection.cursor()
    cursor.execute(
        '''SELECT * FROM `flat` WHERE flat_id=%s;''', (roomId,))
    roomdetails = cursor.fetchall()
    print("hskhgfhsdvjd")
    print(roomdetails)
    cursor.close()
    return render_template('roomdetails.html', result={"room": room, "roomdetails": roomdetails})





    